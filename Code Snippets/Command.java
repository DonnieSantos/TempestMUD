class Command

{
  /////////////////////////////////////////////////////////////////////////////
  /////////////////////////////////////////////////////////////////////////////

  public static void execute(Data entity, String command)

  {
    String commandName = Utility.first(command);
    String parameters = Utility.last(command);
    CommandTable.getInstance().get(commandName).execute(entity, parameters);
  }

  /////////////////////////////////////////////////////////////////////////////
  /////////////////////////////////////////////////////////////////////////////
}